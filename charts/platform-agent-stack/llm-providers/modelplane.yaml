# Modelplane — self-hosted, OpenAI-compatible inference. Rename to
# modelplane.yaml to enable.
provider: modelplane
type: openai-compatible
endpoint: ${MODELPLANE_ENDPOINT}
apiKey: ${MODELPLANE_API_KEY}
models:
  default: ${MODELPLANE_MODEL}
