{
  "itsm": {
    "url": "https://mcp.atlassian.com/v1/mcp",
    "headers": {
      "Authorization": "Bearer ${ATLASSIAN_API_KEY}"
    },
    "injectArgs": {
      "cloudId": "${ATLASSIAN_CLOUD_ID}",
      "projectKey": "${JSM_PROJECT_KEY}"
    },
    "_endpoint": "Atlassian's hosted Rovo MCP Server, Streamable HTTP. NOT /v1/sse - that endpoint stopped being supported on 2026-06-30 (support.atlassian.com/atlassian-rovo-mcp-server/docs/configuring-authentication-via-api-token/). This file previously named /v1/sse with \"transport\": \"sse\", which was wrong twice over: the endpoint is retired AND the bridge has no SSE transport, only stdio and Streamable HTTP. Verified against Atlassian's docs 2026-08-18.",
    "_auth": "Service-account API key as a Bearer token. Atlassian's OAuth 2.1 flow is an interactive browser consent, which a pod cannot complete - API-key auth is the documented non-interactive path for 'backend services, CI/CD pipelines, bots, and automated agents'. It must be ENABLED BY AN ORG ADMIN under Security -> Atlassian Rovo MCP Server settings, and the key is created against a service account, not a person. A personal token would work as \"Basic base64(email:token)\" instead, but attributes every ticket the agent raises to that human and carries their whole permission set.",
    "_cloudid": "API-key auth is NOT bound to a cloudId - Atlassian's docs are explicit that clients 'must explicitly pass the cloudId where needed' - so every tool takes it as a REQUIRED argument. The model cannot know it and must not choose it: a model-supplied cloudId is one that prompt injection can redirect, and a ticket written into another Atlassian site is a bad failure. It is injected by the bridge instead (injectArgs, mcpBackends.js), which OVERRIDES whatever the model produced. Discover yours with: curl -s https://<site>.atlassian.net/_edge/tenant_info",
    "_projectkey": "createJiraIssue also requires projectKey and issueTypeName. projectKey is a deployment decision, not a per-request one, so it is injected the same way. issueTypeName is NOT injected - 'Bug' vs 'Task' vs a JSM request type is a genuine per-ticket judgement, and it is carried as an argument_hint in the action mapping instead.",
    "_verified": "2026-08-18: CONNECTS, but the key we were first given exposed NO Jira tools. initialize returned 200 with a session id from inside the pod (so the org's API-token auth toggle is on, the key is valid, and egress works), and tools/list then returned exactly THREE tools - getTeamworkGraphContext, getTeamworkGraphObject, addTeamworkGraphContext. All six mapped verbs were absent, and so was getAccessibleAtlassianResources. Nothing about the mapping was wrong; the SERVICE ACCOUNT KEY had no Jira or Confluence scopes.",
    "_scopes": "THE TOOL LIST IS A FUNCTION OF THE KEY'S SCOPES. Scopes are fixed when the API key is created and cannot be added afterwards - a key missing them has to be regenerated, not edited. The service account also needs product access to Jira Service Management (and Confluence, if the KB verbs are wanted); a key cannot grant a scope for a product the account cannot see. Diagnose by calling tools/list and counting: a short list means scopes, not code. assertVerbsResolve() catches the same thing at boot and names each verb whose tool is missing.",
    "_todo_verify": "STILL NOT PROVEN END TO END. Once a correctly-scoped key is in place: connect, read tools/list, confirm all six mapped names are present, then CALL each mapped verb. A name existing is not proof a verb works - that lesson cost three broken verbs on the Freshservice side, where three tools were listed and every call failed.",
    "_write_surface": "The Rovo server exposes editJiraIssue and transitionJiraIssue, both general-purpose. Neither is mapped, deliberately - see the action mapping for why. An unmapped tool cannot execute regardless of tier."
  }
}
