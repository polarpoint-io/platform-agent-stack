{
  "itsm": {
    "command": "uvx",
    "args": ["--with", "mcp<2", "freshservice-mcp"],
    "_pin": "freshservice-mcp 1.0.0 declares 'mcp[cli]>=1.3.0' with no upper bound, so uvx resolves mcp 2.0.0, which removed mcp.server.fastmcp. The server then dies at import with ModuleNotFoundError before serving anything, and mcpBackends.js reports it as a generic 'Connection closed'. Pinning below 2 resolves mcp 1.29.0 and the server starts. This is a fresh resolve on every pod start, so it will break again the moment any other unbounded dependency of theirs cuts a major - if this backend stops connecting after a restart with no change on our side, check the log for an import error before anything else. Drop the pin once upstream bounds it.",
    "env": {
      "FRESHSERVICE_DOMAIN": "polarpoint.freshservice.com",
      "FRESHSERVICE_APIKEY": "${FRESHSERVICE_APIKEY}"
    },
    "_note": "Self-hosted community server (github.com/effytech/freshservice_mcp, MIT), run via uvx - stdio transport, not a hosted remote endpoint. Freshworks does have its own native MCP integration, but as of writing it's Beta/EAP-gated to selected Enterprise-plan customers (see developers.freshworks.com/docs/agentic-dev-tools/mcp-server/) - this community server works today without needing that program. FRESHSERVICE_APIKEY comes from Profile Settings -> API Settings in the Freshservice admin panel.",
    "_verified": "2026-08-04: all 6 mapped verbs checked against a live tools/list from freshservice-mcp 1.0.0 (59 tools, confirmed identical from the deployed pod) - filter_tickets, get_ticket_by_id, get_list_of_solution_article, get_solution_article, create_ticket_note and create_ticket all exist. Re-run scripts/list-provider-tools.py freshservice after any version bump; it diffs the mapping against the running server and exits non-zero on a miss.",
    "_write_surface": "Those 59 tools include ~20 that write or destroy - delete_ticket, update_ticket, create_service_request, create_agent, create_requester, and the whole solution-article set. Exactly 2 are mapped (create_ticket at tier_3, create_ticket_note at tier_2). The rest have no action-mapping entry and therefore cannot execute at all, which is the intended fail-closed state, NOT an oversight to be tidied up by adding mappings. Note delete_ticket in particular appears in no tier of policy/risk-tiers.yaml either, so it would also fall to default_policy.unlisted_action.",
    "_todo_verify": "If/when Freshworks' own hosted MCP server is available on your plan, it likely follows the same OAuth pattern as Atlassian's (see jira-service-management.mcp.json) and this becomes a url/transport block instead of command/args - the tool names would need re-verifying against THAT server, they are not the same product."
  }
}
