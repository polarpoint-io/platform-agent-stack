# Builds and publishes the MCP bridge image (bridge/) this repo's
# Deployment runs.
name: Build bridge image

on:
  push:
    branches: [main]
    paths:
      - bridge/**
      - .github/workflows/build-bridge-image.yml
  workflow_dispatch: {}

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Read package version
        id: pkg_version
        run: |
          VERSION=$(node -p "require('./bridge/package.json').version")
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: bridge
          push: true
          tags: |
            ghcr.io/polarpoint-io/platform-agent-bridge:${{ steps.pkg_version.outputs.version }}
            ghcr.io/polarpoint-io/platform-agent-bridge:latest
